%h2.title Viewing all Pages

= will_paginate collection

%table
  %thead
    %tr
      %th ID
      %th Key
      %th Title
      %th Published?
      %th &nbsp;
  %tbody
    - collection.each do |r|
      %tr{:class => cycle('odd', 'even')}
        %td= r.id
        %td= r.key
        %td= r.title
        %td= r.published? ? "Yes" : "No"
        %td.actions= individual_resource_links r
    = empty_row_for_collection 4
        
= will_paginate collection