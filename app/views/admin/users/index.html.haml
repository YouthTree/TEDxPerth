%h2.title== Viewing all Users

= will_paginate collection

%table
  %thead
    %tr
      %th ID
      %th Login
      %th Display Name
      %th Last Used Site
      %th &nbsp;
  %tbody
    - collection.each do |r|
      %tr{:class => cycle('odd', 'even')}
        %td= r.id
        %td=vwd r.login
        %td=vwd r.display_name
        %td=l r.last_request_at, :format => :long
        %td.actions= individual_resource_links r
    = empty_row_for_collection 4
        
= will_paginate collection