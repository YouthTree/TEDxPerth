%h2.title Viewing All Banner Items

= will_paginate collection

%table
  %thead
    %tr
      %th Title
      %th Published?
      %th &nbsp;
  %tbody
    - collection.each do |r|
      %tr{:class => cycle('odd', 'even')}
        %td= link_to r.title, [:admin, r]
        %td= r.published?
        %td.actions= individual_resource_links r
    = empty_row_for_collection 2
        
= will_paginate collection