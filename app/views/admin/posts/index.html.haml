%h2.title Viewing all Posts

= will_paginate collection

%table
  %thead
    %tr
      %th Title
      %th Published?
      %th &nbsp;
  %tbody
    - collection.each do |r|
      %tr{:class => cycle('odd', 'even')}
        %td= r.title
        %td= yes_no? r.published?
        %td.actions= individual_resource_links r
    = empty_row_for_collection 3
        
= will_paginate collection